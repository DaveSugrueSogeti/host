package ie.sugrue.repository;

public class JDBCConnectionTemplate {
	/*
	 * protected DataSource dataSource; protected JdbcTemplate jdbcTemplateObject;
	 * 
	 * public void setDataSource(DataSource dataSource) { this.dataSource = dataSource; this.jdbcTemplateObject = new
	 * JdbcTemplate(dataSource); }
	 */
}
